Meteor.startup(function() {

  /*Factory.define('tip', Tips, {
    description: function() { return Fake.sentence(); },
    rating: function() { return _.random(1, 5); }
  });

  if (Tips.find({}).count() === 0) {

    _(10).times(function(n) {
      Factory.create('tip');
    });

  }*/

});
